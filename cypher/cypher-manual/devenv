# source me
if [ -f ./devenv.local ]; then
  . ./devenv.local

  export ARTIFACTORY_URI
  export ARTIFACTORY_USERNAME
  export ARTIFACTORY_PASSWORD

  # Create a symlink to use as the local repo

else
  echo "Couldn't find ./devenv.local."
fi

# vim: set sw=2 ts=2 ft=sh:
